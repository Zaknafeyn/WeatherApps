<Window x:Class="Weather.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        WindowStyle="SingleBorderWindow"
        WindowState="Normal"
        Icon="../Assets/Icon.png"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        
        Height="400" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <ContentControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
            prism:RegionManager.RegionName="HeaderRegion" Padding="10" />

        <ContentControl Grid.Row="1" Grid.Column="0"
            prism:RegionManager.RegionName="MainRegion" Padding="10"/>

        <ContentControl Grid.Row="1" Grid.Column="1" 
            prism:RegionManager.RegionName="SecondaryRegion" Padding="10"/>
    </Grid>
</Window>
