using Android.Support.V4.App;

namespace Weather.AndroidApp.Adapters
{
    public class MainWeatherAdapter : SettingsAdapter
    {
        public MainWeatherAdapter(FragmentManager fm) : base(fm)
        {
        }
    }
}